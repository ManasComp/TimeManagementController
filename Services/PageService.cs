using System.Windows.Forms;

namespace TimeManagementController.Services
{
    public class PageService
    {
        public void Massage(string text)
        {
            MessageBox.Show(text);
        }
    }
}